# Archived Design References

This directory contains exploratory concepts retained for historical/design reference only.

## Logo exploration

The original 12-logo exploration is archived here when the source asset is added.

**Important:** the application must not select from this directory. The approved active identity is **Logo Option #11**, stored under `design/brand/`.

Archived concepts are not active product assets and should not be imported into application code.
